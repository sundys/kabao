# v1.0.16

## 修复

- 修复证件 CSV 导入报“category_type 证件模板必须使用 document”的问题：分类类型列现在与模板转换脚本保持一致，接受 `document`、`证件`、`证件卡` 三种写法。
- 银行卡分类类型同步扩展中文别名：`借记卡`、`借记`、`储蓄卡`、`信用卡`、`贷记卡` 均可导入。
- 校验失败提示更新为列出全部可接受的取值，便于按提示修正表格。

## 安全与数据

- 仅调整导入校验的取值别名，不改变数据库结构、加密格式或备份兼容性。
- 已通过 flutter analyze 与 154 项单元/组件测试。
