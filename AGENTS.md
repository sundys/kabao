# AGENTS.md — 本地加密卡包（Flutter）开发规范

## 1. 文档目的与优先级

本文件是本仓库内所有开发者与自动化 Agent 的执行规范，用于实现“本地加密银行卡及身份证信息管理”Android 应用。修改代码、依赖、构建配置、CI/CD 或文档前，必须遵守本文件。

规则优先级从高到低：用户明确要求 > 安全与隐私要求 > 本文件 > 现有项目约定 > 通用最佳实践。需求不明确时，先采用隐私保护更强、数据不丢失且可回滚的方案；若会影响数据格式、加密方案或用户体验，必须说明假设并请求确认。

## 2. 产品范围

### 2.1 技术范围
- 当前系统已经配置好所有Flutter及android开发SDK变量，可以直接测试。
- 客户端：Flutter，首期仅 Android；代码保持跨平台兼容，除非功能确实仅支持 Android。
- 数据：所有业务数据仅加密存储在用户设备本地；不接入业务后端、不上传分析数据、不使用云推送。
- 备份：用户主动导入/导出加密备份，或配置 WebDAV 后主动/按用户启用的策略备份。
- 发布：GitHub 仓库与 GitHub Actions 构建；签名文件及口令仅存 GitHub Secrets/环境变量，绝不提交仓库。系统已经配置ssh及ssh别名,远程仓库链接：git remote add origin git@wapxw:sundys/kabao.git，可直接用git push -u origin master命令推送。

### 2.2 功能范围
- 应用包名为com.sundys.kabao,应用名称中文卡包，英文为Card Pack
- 首次启动：设置主密码；可选启用设备生物识别（指纹）登录。
- 首页底部导航：卡包、通知、设置。
- 卡包：借记卡与信用卡两个可滑动切换的标签；银行分类（最多 4 个中文字符）；卡片新增、查看、编辑、复制卡号。
- 卡片字段：卡号（最多 19 位，界面按 4 位分组显示，复制为无空格连续数字）、有效期（`MM/YY`）、CVV、U 盾证书到期日（`yyyy/M/d`）、备注。
- 通知：本地持久化保存；支持已读、删除、取消；根据有效期与 U 盾日期生成本地提醒。
- 设置：修改密码、生物识别登录、导入、导出、WebDAV 设置、清空全部数据。
- 配色：每添一个银行分类，都用不同颜色的浅色显示，以便区分。整体颜色搭配要合适，请根据你的经验进行整体颜色匹配，尽量更美观更现代化。
任何新增联网、账户体系、远程同步、遥测、广告或第三方统计功能均不在当前范围内，必须先获用户明确授权。

## 3. 安全与隐私（不可妥协）

### 3.1 敏感数据规则

- 卡号、身份证号码、CVV、证书日期、备注、备份内容、WebDAV 凭据、主密码及其派生材料均为敏感数据。
- 敏感数据不得以明文写入数据库、SharedPreferences/DataStore、日志、崩溃报告、截图、剪贴板历史、测试快照、CI 输出或 Git。
- 禁止 `print`、`debugPrint`、日志框架记录敏感值；错误信息使用字段名、内部错误码或已脱敏摘要。
- UI 默认不展示超过必要的完整信息：列表显示应脱敏（例如 `5555 **** **** 4456`）；详情页仅在已解锁状态下展示完整值。截图预览、多任务切换界面应使用隐私遮罩或敏感页遮挡。
- 复制行为仅允许“卡号”这一明确功能；复制内容为无空格数字。复制后显示提示，并在应用失焦或短时延后尽力清除自家写入的剪贴板内容，不得承诺能清除系统历史。
- 绝不收集、传输或上报用户金融/身份数据。网络请求仅限用户明确配置的 WebDAV 地址。

### 3.2 密码、密钥与加密

- 主密码绝不保存为明文、可逆密文或简单哈希。使用带随机盐的内存困难 KDF（优先 Argon2id；如受平台限制须记录原因并采用 PBKDF2-HMAC-SHA-256 的高成本参数）。
- 业务数据库与导出备份必须使用经认证加密（AEAD，例如 AES-256-GCM 或 ChaCha20-Poly1305），每次加密使用唯一随机 nonce/IV，并保存版本、盐、KDF 参数与认证标签。
- 数据加密密钥由主密码派生或被 Android Keystore 包装；密钥材料不可写入日志或普通偏好存储。使用成熟、维护活跃的库，不自行实现密码学算法。
- 生物识别只用于从 Android Keystore 解封已存在的密钥或恢复已验证会话，不能替代主密码的加密安全边界；必须保留主密码登录与恢复路径。
- 修改主密码必须以事务/双阶段迁移方式重新包装或重加密密钥；成功前不得删除旧数据，失败后必须可继续用旧密码打开。
- 导入前验证文件版本、完整性和认证标签；认证失败不得写入现有库。导入采取预校验、暂存、用户确认、原子替换/合并、可恢复失败的流程。
- WebDAV 凭据必须存储在安全存储/Keystore 保护区域，导出日志和诊断信息不得包含 URL 中的账号、密码或 token。

### 3.3 会话与应用生命周期

- 应用启动、从后台恢复、进入敏感详情或执行导入导出前，均检查锁定状态；默认后台立即遮罩并在可配置的短超时后重新验证。
- 连续认证失败须采用渐进延迟，避免暴力尝试；不得在本地保存可被利用的明文失败记录。
- 清空全部数据是不可逆操作：必须二次确认、明确告知本地卡片/通知/配置将被删除，并在操作后删除密钥与数据库文件。执行前提示用户先导出备份。

## 4. 架构与目录约定

采用按功能划分、分层清晰的 Flutter 架构，避免页面直接访问数据库或平台 API。推荐目录如下；项目建立后按此结构落位：

```text
lib/
  app/                 # App 根组件、路由、主题、依赖装配
  core/                # 通用错误、结果类型、安全、数据库、平台服务
  features/
    auth/              # 首次设置、密码验证、生物识别、锁定状态
    wallet/            # 分类、借记卡/信用卡、卡片详情与复制
    notifications/     # 提醒计算、通知存储、通知界面
    backup/            # 加密备份、导入导出、WebDAV
    settings/          # 设置及危险操作入口
  shared/              # 无业务归属的 UI 组件、格式化与校验工具
test/
integration_test/
android/
.github/workflows/
docs/
```

- 表现层：页面、Widget、状态管理；只调用用例，不拼接 SQL、加密或 HTTP。
- 领域层：实体、值对象、仓储接口、业务用例；不得依赖 Flutter UI、数据库或插件实现。
- 数据层：加密数据库、DTO、仓储实现、WebDAV 客户端；数据进出持久层时统一加解密与版本迁移。
- 依赖方向只能由外层指向内层。插件调用封装在 `core` 或数据层，便于测试和替换。
- 选择一种状态管理方案并全项目统一（推荐 Riverpod）；禁止同一功能混用 Provider、BLoC、GetX 等多套方案。

## 5. 数据模型与业务规则

### 5.1 基础实体

为每条记录建立不可变的 UUID、创建时间、更新时间和模型版本。建议最小实体：

- `BankCategory`：`id`、`cardType`（debit/credit）、`name`、排序字段、时间戳。
- `CardRecord`：`id`、`categoryId`、`cardType`、`cardNumber`、`expiryMonth`、`expiryYear`、`cvv`、`uShieldExpiryDate`、`note`、时间戳。
- `AppNotification`：`id`、`type`、`cardId`、`scheduledFor`、`createdAt`、`readAt`、`deletedAt`（或物理删除）、展示标题和内容快照。
- `BackupProfile`：只保存必要的 WebDAV 非秘密配置；凭据引用安全存储键名，不保存凭据明文。

禁止仅以卡号作为主键。列表排序、数据合并与导入冲突必须基于 UUID 和时间戳；发生冲突时向用户展示明确策略，默认不静默覆盖。

### 5.2 输入校验与展示

- 银行分类名称：去除首尾空白后不能为空，最多 4 个中文字符；实现时按 Unicode 字素簇计数，并拒绝非中文字符，错误文案清楚说明规则。
- 卡号：保存前移除空格与常见分隔符，仅允许数字，长度 1–19；若启用 Luhn 校验，只能作为提示或在需求确认后强制，避免拒绝非标准卡号。
- 卡号显示按每 4 位分组，最后一组可不足 4 位；复制和持久化值不得包含空格。
- 有效期必须为 `MM/YY`，月份 `01`–`12`，年份按 2000 + `YY` 转换；用于通知时以该月最后一天为到期日，并将规则单元测试固化。
- CVV 仅允许 3–4 位数字；若当前产品强制 3 位，需在产品文档中明确，不要悄然截断输入。
- U 盾到期日使用本地日期类型（不带时区），接受并规范化为 `yyyy/M/d`；非法或不存在日期必须拒绝。
- 备注设置合理长度上限并保留换行；不得因 UI 截断而丢失原内容。

## 6. 页面与交互规范

### 6.1 认证流程

首次启动显示密码创建与确认页，说明密码无法由服务端找回；只有写入成功后才标记初始化完成。后续启动先进入锁定页，可按用户设置提供生物识别入口。认证取消、失败、设备不支持时始终可使用密码路径。

### 6.2 卡包

- 首页第一个导航项为“卡包”，顶部使用借记卡/信用卡双标签并支持点击与左右滑动切换，状态在返回时保留。
- 每一类卡片类型下由用户创建银行分类；分类进入后新增记录。分类删除前提示其中卡片数量并要求确认。
- 卡片列表一行一条，默认显示脱敏且分组的卡号，右侧有单独的复制图标。点击行进入详情，不因点击复制图标而打开详情。
- 详情页显示所有字段，支持编辑、保存和取消；保存前校验，编辑失败保留用户输入且不覆盖原数据。

### 6.3 通知

- 通知中心是第二个导航项，消息在本地持久化且默认永不自动删除。
- 长按通知提供“删除”“标为已读”“取消”三个操作；取消无状态改变，删除必须有防误触确认或可撤销提示。
- 提醒在应用启动、恢复、卡片创建/修改、以及本地定时任务触发时进行幂等计算。生成相同卡片、类型、提醒档位与到期日的通知时不得重复插入。
- 卡片有效期与 U 盾证书日期均应在到期前 90、60、30、15 天各提醒一次；原始描述“按到期前的15天30天60天90天三次”存在数量矛盾，本规范按四次实现。如要改为三次，须先确认保留哪些档位。
- 本地通知权限被拒绝时，通知中心仍应生成并保存应用内消息；设置页说明系统横幅提醒不可用。

### 6.4 设置与备份

- 设置是第三个导航项，包含修改密码、生物识别开关、导入、导出、WebDAV 备份设置、清空全部数据，关于子页面。关于此页面用于介绍应用功能以及开源主页，检测更新，检测更新更新，不主动检测，等用户手动检查才会检测，检测github/sundys/kaobao项目中的最新版本。
- 导出前要求用户再次验证主密码或当前已验证会话，并让用户选择保存位置与备份密码/密钥策略。备份文件必须独立加密，不能仅复制数据库文件。
- 导入前显示备份元数据（格式版本、创建时间，且不泄露敏感内容）并要求用户确认导入模式；不得自动上传或覆盖。
- WebDAV 需提供地址、用户名、密码/令牌、远程目录及“测试连接”功能。测试成功不等于自动备份；自动备份开关、网络条件、频率和覆盖策略必须显式呈现。

## 7. 备份格式与兼容性

- 备份采用版本化、自描述的加密容器，例如包含未敏感的格式版本、创建时间、KDF 参数、盐、nonce、密文与认证标签；实际业务 JSON/数据库内容全部置于密文中。
- 格式必须在 `docs/backup-format.md` 记录，包含版本迁移、兼容窗口和错误处理。改变格式时增加版本，不破坏旧版本导入。
- 导出使用临时文件后原子移动到目标；异常、取消或认证失败时清理临时文件。Android 存储访问框架（SAF）优先，避免申请不必要的全盘存储权限。
- WebDAV 使用 HTTPS；HTTP 仅在开发环境且用户明确确认风险时允许。校验证书，设置合理超时、重试和网络错误提示；不得绕过 TLS 校验。

## 8. Android 与 Flutter 实现要求

- 支持的 `minSdk`、`targetSdk`、Flutter/Dart 版本集中声明并在 README 中维护；升级依赖前检查 Android 兼容性和安全公告。
- 仅申请最小权限。通常需要通知权限（Android 13+）和生物识别能力；文件操作优先 SAF，不申请宽泛存储权限。
- 使用 `local_auth` 等成熟插件调用生物识别，使用 `flutter_secure_storage` 或平台等价方案保管 Keystore 保护的秘密；具体依赖须锁定版本并评估维护状态。
- 本地通知使用支持 Android 13+ 权限与时区的实现。精确闹钟权限仅在确有必要并完成合规说明后申请；默认采用系统允许的近似调度，并在每次启动/恢复重新校验提醒。
- 处理深色模式、字体缩放、屏幕旋转、无障碍语义、键盘遮挡和离线环境；不要把颜色、间距或中文文案散落在业务 Widget 中。

## 9. 代码质量与变更规则

- 使用空安全，新增公开 API 必须有类型、注释和错误语义；优先小而可测试的纯函数。
- 统一使用 `dart format`、`flutter analyze`，禁止提交分析错误、死代码、未使用依赖和被忽略的安全警告。
- 不在业务代码中硬编码密钥、密码、WebDAV 地址、签名信息、包名环境差异或魔法数字；放入类型安全的配置、常量或构建变量。
- 依赖新增必须说明用途、许可证、维护状况、最小权限和替代方案；避免重复功能库。更新依赖后运行完整测试。
- 数据库 schema、加密封装、备份格式、认证流程和 CI 密钥处理属于高风险区域：任何改动都必须包含迁移/回滚考虑、测试及代码审查说明。
- Git 提交保持单一目的，不提交 `*.jks`、`key.properties`、`.env`、备份文件、数据库、私钥、签名产物或包含真实数据的截图。相应规则写入 `.gitignore`。

## 10. 测试与验收门槛

每次功能改动至少运行：

```powershell
flutter format --set-exit-if-changed .
flutter analyze
flutter test
```

涉及 Android 平台、通知、SAF、生物识别、数据库迁移、导入导出或 WebDAV 时，还必须在真机或模拟器执行对应 `integration_test`，并说明设备/API 级别与结果。

最低测试覆盖：

- 单元测试：字段校验、卡号格式化/复制、到期日计算、四个提醒档位、去重、脱敏、KDF/加密封装接口、备份版本解析。
- 仓储测试：加密写入后可读、错误密码/篡改密文失败、迁移可恢复、导入不覆盖失败、清空后不可读取。
- Widget 测试：首次设置、锁定/解锁、双标签切换、卡片新增编辑、通知长按菜单、危险操作确认。
- 集成测试：首次安装到解锁、创建卡片、生成通知、加密导出再导入、WebDAV 测试连接（使用隔离测试服务或 mock）。
- 安全回归：确认日志/异常/测试输出不含真实卡号、CVV、身份证号、密码、密钥或 WebDAV 凭据。

## 11. GitHub Actions 与发布

- 工作流置于 `.github/workflows/`，PR 至少执行格式检查、静态分析、单元测试与 debug APK 构建；缓存 Flutter/Gradle，但不缓存密钥或解密后的文件。
- Release 工作流仅在受保护分支的版本标签触发，构建签名 AAB/APK。签名 keystore 以 Base64 等形式保存于 GitHub Secrets，运行时写入临时路径，构建结束后删除。
- 仅通过 GitHub Secrets 注入 `KEYSTORE_BASE64`、`KEYSTORE_PASSWORD`、`KEY_ALIAS`、`KEY_PASSWORD` 等敏感变量；禁止在命令行回显、`set -x`、构建日志或 artifact 中暴露。
- 工作流使用固定版本或 commit SHA 的第三方 Action；限制 `GITHUB_TOKEN` 到最小权限；依赖更新和发布需要人工审核。
- 发布前核对 applicationId、versionName/versionCode、权限声明、签名、ProGuard/R8、隐私说明、升级兼容性和回滚包。不要将含调试信息或真实备份的 artifact 对外发布。

## 12. 开发流程与交付清单

按以下顺序实施，前一阶段的安全和测试基线通过后再进入下一阶段：

1. 初始化 Flutter 工程、CI、目录结构、主题、路由、依赖注入和代码质量工具。
2. 实现密钥管理、加密数据库、锁定状态与首次密码设置，并完成加密/篡改测试。
3. 实现卡包分类与卡片 CRUD、输入校验、脱敏展示、复制和详情编辑。
4. 实现到期规则、应用内通知持久化与本地系统提醒。
5. 实现导入导出、备份格式和数据恢复验证。
6. 实现 WebDAV 配置、连通性测试与显式备份策略。
7. 完善设置、无障碍、异常恢复、集成测试、签名发布工作流与用户文档。

每个 PR/交付必须说明：功能范围、数据与安全影响、迁移策略、已执行命令和结果、手工验证项、已知限制。出现认证失败、数据损坏、导入异常或网络错误时，应提供可理解且不泄露敏感信息的提示，并保留原始数据直到用户明确选择恢复或删除。

## 13. 禁止事项

- 禁止以明文或可逆弱加密保存、上传、打印、测试或提交任何敏感数据。
- 禁止自行发明加密算法、固定 IV/nonce、复用 nonce、将密码当作加密密钥直接使用。
- 禁止在用户未确认时覆盖、删除、合并或上传备份与业务数据。
- 禁止为方便开发而关闭 TLS 验证、绕过生物识别认证、跳过导入完整性验证或忽略分析/测试失败。
- 禁止把生产签名材料、真实 WebDAV 凭据、真实卡号/身份证号写进源码、示例、Issue、PR 或 CI 日志。

